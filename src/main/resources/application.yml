spring:
  datasource:
    url: ${DB_URL}
    username: ${USER_NAME}
    password: ${PASSWORD}
    driver-class-name: ${DRIVER_NAME}


  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

  r2dbc:
    url: ${RDB_URL}
    name: ${RDB_NAME}
    username: ${USER_NAME}
    password: ${PASSWORD}

  jpa:
    hibernate:
      ddl_auto: update
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100
        open-in-view: false

  ai:
    openai:
      api-key: ${OPEN_AI_ACCESS_KEY}
      gpt-url: ${OPEN_AI_GPT_URL}
      dall-e-url: ${OPEN_AI_DALL_E_URL}
      dall-e-url-for-variation: ${OPEN_AI_DALL_E_URL_FOR_VARIATION}
      base-url: ${OPEN_AI_BASE_URL}

  mvc:
    async:
      request-timeout: 300000

logging:
  level:
    org.hibernate.SQL: info
    org.hibernate.type: trace
    org:
      springframework:
        r2dbc: DEBUG
        orm:
          jpa: DEBUG
        transaction: DEBUG

aws:
  s3:
    access-key: ${AWS_ACCESS_KEY}
    secret-key: ${AWS_SECRET_KEY}
    bucket: ${AWS_BUCKET_NAME}
    region: ${AWS_REGION}
jwt:
  issuer: wookdong
  secret-key: c1b94a4daddfe04955067b09032a52c6e7b9433a5af91efbde4b44f69e99546e5713e30e4c5e5f2f22c21188f641d50fa97425724f2b8f0ad3d47c7d6bad1a6f

  port: 8081
  servlet:
    session:
      cookie:
        same-site: none
        secure: true